import React, {Component} from 'react';
import {View,Text} from "react-native";

const todo = props => {
    return (
        <View>
            <Text>
                TO DO COMPONENTS
            </Text>
        </View>
    );
};

export default todo;
